export default class Release {
    constructor(mbid, title, artist, type, year) {
        this.mbid = mbid;
        this.title = title;
        this.artist = artist;
        this.type = type;
        this.year = year;
    }
}